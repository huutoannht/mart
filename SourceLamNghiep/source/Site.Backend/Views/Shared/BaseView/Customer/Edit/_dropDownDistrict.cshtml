@model Models.Customer.CustomerModel
@{
    var districts = SiteUtils.GetVietNamDistricts(Model.City);
}

@Html.DropDownListFor(m => m.District, districts.ToSelectList(), BackendMessage.Select, new { @class = "form-control chosen" })