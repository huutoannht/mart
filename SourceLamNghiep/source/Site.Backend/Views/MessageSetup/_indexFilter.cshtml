@model Models.HtmlContent.HtmlContentModel

<div class="row">
    <div class="col-md-12 col-md-12">
        <div class="panel panel-blue">
            <div class="panel-heading">
                @BackendMessage.Filter
            </div>
            <div class="panel-body">
                @using (Html.BeginForm("Index", "MessageSetup", FormMethod.Post, new { id = "formFilter", role = "form", @class = "form-horizontal" }))
                {
                    @Html.ValidationSummary(string.Empty)
                    <div class="form-group">
                        @Html.LabelFor(m => m.Type, new { @class = "col-md-2 control-label" })
                        <div class="col-md-10">
                            @Html.DropDownListFor(m => m.Type, (typeof(MessageCase)).EnumToList().ToSelectList(), BackendMessage.Select, new { @class = "form-control chosen" })
                        </div>
                    </div>
                    <div class="form-group">
                        @Html.LabelFor(m => m.LanguageCode, new { @class = "col-md-2 control-label" })
                        <div class="col-md-4">
                            @Html.DropDownListFor(m => m.LanguageCode, SiteUtils.LanguageList().ToSelectList(), BackendMessage.Select, new { @class = "form-control chosen" })
                        </div>
                    </div>
                        
                    <div class="form-actions">
                        <div class="col-md-offset-2 col-md-10">
                            <button onclick="htmlContent.searchClick()" class="btn btn-success" type="button">@BackendMessage.Search</button>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>
